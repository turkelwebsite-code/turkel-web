---
// REDIRECT TO DYNAMIC PAGE
import { getCollection } from 'astro:content';

const fairYears = await getCollection('fair-years');
const year2025Data = fairYears.find(item => item.id === '2025');
const dynamicYear = year2025Data?.data?.year || 2025;

console.log('🔄 REDIRECT: 2025 page redirecting to:', `${dynamicYear}-fuar-takvimi`);
return Astro.redirect(`/tr/${dynamicYear}-fuar-takvimi/`);
---