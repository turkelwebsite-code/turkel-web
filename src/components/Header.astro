---
import { NAVIGATION_ITEMS, SITE_CONFIG } from '../lib/constants';
import { getLocaleFromUrl, type Locale } from '../lib/i18n';
import { Phone, Mail, Facebook, Youtube, Instagram, Linkedin } from 'lucide-astro';
import LangSwitch from './LangSwitch.astro';
import VisitorFormModal from './VisitorFormModal.astro';
import ParticipantFormModal from './ParticipantFormModal.astro';
import { getCollection } from 'astro:content';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;

// Load active fair years for dynamic menu
const fairYears = await getCollection('fair-years');
const activeFairYears = fairYears
  .filter(year => year.data.is_active && year.data.display_status === 'Aktif Fuarlar')
  .sort((a, b) => (a.data.menu_order || 1) - (b.data.menu_order || 1));

console.log('🎯 HEADER: Active fair years loaded:', activeFairYears.length);
const navigation = NAVIGATION_ITEMS[locale];
const currentPath = Astro.url.pathname;
---

<header class="w-full sticky top-0 z-50 bg-white shadow-sm">
  <!-- Top Contact Bar - Hidden on Mobile -->
  <div class="py-3 hidden lg:block">
    <div class="mx-auto px-5" style="max-width: calc(100% - 20px); background-color: #1B1C1B; border-radius: 10px;">
      <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-4 text-white py-3 px-6">
        <!-- Contact Info -->
        <div class="flex flex-col sm:flex-row sm:items-center gap-4 text-sm">
          <div class="flex items-center gap-2">
            <Phone size={16} />
            <span>{SITE_CONFIG.contact.phone}</span>
          </div>
          <span class="hidden sm:inline text-gray-400">|</span>
          <div class="flex items-center gap-2">
            <Mail size={16} />
            <span>{SITE_CONFIG.contact.email}</span>
          </div>
        </div>
        
        <!-- Social Media -->
        <div class="flex items-center gap-3">
          <a 
            href={SITE_CONFIG.social.facebook}
            class="w-8 h-8 rounded-full border border-white/20 flex items-center justify-center hover:bg-white/10 transition-colors"
            aria-label="Facebook"
          >
            <Facebook size={14} />
          </a>
          <a 
            href={SITE_CONFIG.social.youtube}
            class="w-8 h-8 rounded-full border border-white/20 flex items-center justify-center hover:bg-white/10 transition-colors"
            aria-label="YouTube"
          >
            <Youtube size={14} />
          </a>
          <a 
            href={SITE_CONFIG.social.instagram}
            class="w-8 h-8 rounded-full border border-white/20 flex items-center justify-center hover:bg-white/10 transition-colors"
            aria-label="Instagram"
          >
            <Instagram size={14} />
          </a>
          <a 
            href={SITE_CONFIG.social.linkedin}
            class="w-8 h-8 rounded-full border border-white/20 flex items-center justify-center hover:bg-white/10 transition-colors"
            aria-label="LinkedIn"
          >
            <Linkedin size={14} />
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Main Navigation -->
  <div class="bg-white">
    <div class="mx-auto px-4 lg:px-8" style="max-width: calc(100% - 20px);">
      <div class="flex items-center py-2 lg:py-3 relative">
        <!-- Mobile: Logo Left -->
        <div class="flex items-center lg:hidden">
          <a href={`/${locale}/`} class="flex items-center">
            <img 
              src="/images/turkel-logo-mobile.png" 
              alt="Türkel Fuarcılık"
              width="140"
              height="63"
              class="h-12 w-[120px]"
              style="display: block !important; visibility: visible !important;"
            />
          </a>
        </div>

        <!-- Desktop: Logo Left -->
        <div class="hidden lg:flex flex-shrink-0">
          <a href={`/${locale}/`} class="flex items-center">
            <img 
              src="/images/turkel-logo-mobile.png" 
              alt="Türkel Fuarcılık"
              width="180"
              height="81"
              class="h-18 w-[162px] ml-5"
              style="display: block !important; visibility: visible !important;"
            />
          </a>
        </div>

        <!-- Center Navigation -->
        <nav class="hidden lg:flex items-center justify-center flex-1" role="navigation">
          <div class="flex items-center space-x-8">
            {navigation.map((item) => (
              <div class="relative group">
                {/* Check if item has dropdown using hasDropdown property */}
                {(item as any).hasDropdown ? (
                  /* Dropdown trigger - no link */
                  <button 
                    type="button"
                    class="text-base font-normal text-text-primary hover:text-[#CE1A28] transition-all duration-300 uppercase tracking-wide whitespace-nowrap flex items-center gap-1 py-2 px-1 relative cursor-pointer bg-transparent border-0"
                  >
                    {item.label}
                    <svg class="w-3 h-3 transition-transform duration-300 group-hover:rotate-180" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
                    </svg>
                    
                    <!-- Hover underline animation -->
                    <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-[#CE1A28] transition-all duration-300 group-hover:w-full"></span>
                  </button>
                ) : (
                  /* Regular link - no dropdown */
                  <a 
                    href={item.href}
                    class="text-base font-normal text-text-primary hover:text-[#CE1A28] transition-all duration-300 uppercase tracking-wide whitespace-nowrap flex items-center gap-1 py-2 px-1 relative"
                    onclick={`console.log('Header link clicked:', '${item.label}', '${item.href}'); return true;`}
                  >
                    {item.label}
                    
                    <!-- Hover underline animation -->
                    <span class="absolute bottom-0 left-0 w-0 h-0.5 bg-[#CE1A28] transition-all duration-300 group-hover:w-full"></span>
                  </a>
                )}
                
                {/* Dropdown Menus */}
                {(item as any).hasDropdown && item.label === (locale === 'tr' ? 'KURUMSAL' : 'CORPORATE') && (
                  <div class="absolute top-full left-0 mt-0 w-48 bg-white  shadow-xl border border-gray-200 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 z-[9999] transform translate-y-2 group-hover:translate-y-0">
                    <div class="py-2">
                      <a href={`/${locale}/${locale === 'tr' ? 'hakkimizda' : 'about-us'}/`} class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-[#CE1A28] transition-colors">
                        {locale === 'tr' ? 'Hakkımızda' : 'About Us'}
                      </a>
                      <a href={`/${locale}/${locale === 'tr' ? 'kronoloji' : 'chronology'}/`} class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-[#CE1A28] transition-colors">
                        {locale === 'tr' ? 'Kronoloji' : 'Chronology'}
                      </a>
                      <a href={`/${locale}/${locale === 'tr' ? 'ekibimiz' : 'our-team'}/`} class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-[#CE1A28] transition-colors">
                        {locale === 'tr' ? 'Ekibimiz' : 'Our Team'}
                      </a>
                    </div>
                  </div>
                )}
                
                {(item as any).hasDropdown && item.label === (locale === 'tr' ? 'FUARLARIMIZ' : 'OUR FAIRS') && (
                  <div class="absolute top-full left-0 mt-0 w-56 bg-white  shadow-xl border border-gray-200 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 z-[9999] transform translate-y-2 group-hover:translate-y-0">
                    <div class="py-2">
                      {activeFairYears.map(fairYear => {
                        const slug = locale === 'tr' 
                          ? (fairYear.data.page_slug || `${fairYear.data.year}-fuar-takvimi`)
                          : (fairYear.data.page_slug_en || `${fairYear.data.year}-fair-calendar`);
                        const menuTitle = locale === 'tr' 
                          ? (fairYear.data.page_content?.menu_title_tr || `${fairYear.data.year} Fuar Takvimi`)
                          : (fairYear.data.page_content?.menu_title_en || `${fairYear.data.year} Fair Calendar`);
                        
                        return (
                          <a href={`/${locale}/${slug}/`} class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-[#CE1A28] transition-colors">
                            {menuTitle}
                          </a>
                        );
                      })}
                      <div class="border-t border-gray-200 my-1"></div>
                      <a href={`/${locale}/${locale === 'tr' ? 'galeri' : 'gallery'}/`} class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-[#CE1A28] transition-colors">
                        {locale === 'tr' ? 'Galeri' : 'Gallery'}
                      </a>
                    </div>
                  </div>
                )}
                
                {(item as any).hasDropdown && item.label === (locale === 'tr' ? 'HİZMETLERİMİZ' : 'SERVICES') && (
                  <div class="absolute top-full left-0 mt-0 w-48 bg-white  shadow-xl border border-gray-200 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 z-[9999] transform translate-y-2 group-hover:translate-y-0">
                    <div class="py-2">
                      <a href={`/${locale}/${locale === 'tr' ? 'nakliye' : 'shipping'}/`} class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-[#CE1A28] transition-colors">
                        {locale === 'tr' ? 'Nakliye' : 'Shipping'}
                      </a>
                      <a href="https://turkelglobalstands.com" target="_blank" rel="noopener noreferrer" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-[#CE1A28] transition-colors">
                        {locale === 'tr' ? 'Stant Hizmeti' : 'Booth Service'}
                      </a>
                      <a href={`/${locale}/${locale === 'tr' ? 'turizm' : 'tourism'}/`} class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-[#CE1A28] transition-colors">
                        {locale === 'tr' ? 'Turizm' : 'Tourism'}
                      </a>
                    </div>
                  </div>
                )}
                
                {(item as any).hasDropdown && item.label === (locale === 'tr' ? 'FAYDALI LİNKLER' : 'USEFUL LINKS') && (
                  <div class="absolute top-full left-0 mt-0 w-56 bg-white  shadow-xl border border-gray-200 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 z-[9999] transform translate-y-2 group-hover:translate-y-0">
                    <div class="py-2">
                      <a href="https://www.ticaret.gov.tr/yurtdisi-teskilati" target="_blank" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-[#CE1A28] transition-colors">
                        {locale === 'tr' ? 'Müşavirlikler' : 'Consultancies'}
                      </a>
                      <a href="https://www.mfa.gov.tr/yurtdisi-teskilati.tr.mfa" target="_blank" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-[#CE1A28] transition-colors">
                        {locale === 'tr' ? 'Büyükelçilikler' : 'Embassies'}
                      </a>
                      <a href="https://www.ticaret.gov.tr/" target="_blank" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-[#CE1A28] transition-colors">
                        {locale === 'tr' ? 'T.C. Ticaret Bakanlığı' : 'Ministry of Trade'}
                      </a>
                    </div>
                  </div>
                )}
              </div>
            ))}
          </div>
        </nav>

        <!-- Right Side: CTA Buttons & Language Switcher -->
        <div class="hidden lg:flex items-center gap-3" style="margin-right: 20px;">
          <button 
            onclick="openVisitorModal()"
            class="btn btn-primary text-xs px-5 py-2 whitespace-nowrap"
          >
            {locale === 'tr' ? 'Ziyaretçi Formu' : 'Visitor Form'}
          </button>
          <button 
            onclick="openParticipantModal()"
            class="btn btn-outline text-xs px-5 py-2 whitespace-nowrap"
          >
            {locale === 'tr' ? 'Katılımcı Formu' : 'Participant Form'}
          </button>
          <div class="ml-3">
            <LangSwitch currentLocale={locale} currentPath={currentPath} />
          </div>
        </div>

        <!-- Mobile Menu Button -->
        <button 
          id="mobile-menu-button"
          class="lg:hidden absolute right-0 top-1/2 transform -translate-y-1/2 flex items-center justify-center w-10 h-10 rounded-md border border-gray-200 bg-white"
          aria-label="Toggle menu"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Navigation -->
  <div id="mobile-menu" class="lg:hidden hidden bg-white border-t border-gray-200">
    <div class="container mx-auto px-container py-8">
      <nav class="flex flex-col space-y-3" role="navigation">
        {navigation.map((item) => (
          <div class="mobile-nav-item">
            <div class="flex items-center justify-between">
              {/* Check if item has dropdown using hasDropdown property */}
              {(item as any).hasDropdown ? (
                /* Dropdown trigger - no link */
                <span 
                  class="text-base font-normal text-text-primary hover:text-primary transition-colors uppercase tracking-wide py-2 flex-1 cursor-pointer"
                  onclick={`toggleMobileDropdown('${
                    item.label === (locale === 'tr' ? 'KURUMSAL' : 'CORPORATE') ? 'kurumsal' :
                    item.label === (locale === 'tr' ? 'FUARLARIMIZ' : 'OUR FAIRS') ? 'fuarlarimiz' :
                    item.label === (locale === 'tr' ? 'HİZMETLERİMİZ' : 'SERVICES') ? 'hizmetlerimiz' :
                    'faydali-linkler'
                  }')`}
                >
                  {item.label}
                </span>
              ) : (
                /* Regular link - no dropdown */
                <a 
                  href={item.href}
                  class="text-base font-normal text-text-primary hover:text-primary transition-colors uppercase tracking-wide py-2 flex-1"
                  onclick={`console.log('Mobile link clicked:', '${item.label}', '${item.href}'); return true;`}
                >
                  {item.label}
                </a>
              )}
              {/* Mobile dropdown toggle */}
              {(item as any).hasDropdown && (
                <button 
                  class="mobile-dropdown-toggle p-2 text-gray-400 hover:text-primary transition-colors"
                  onclick={`toggleMobileDropdown('${
                    item.label === (locale === 'tr' ? 'KURUMSAL' : 'CORPORATE') ? 'kurumsal' :
                    item.label === (locale === 'tr' ? 'FUARLARIMIZ' : 'OUR FAIRS') ? 'fuarlarimiz' :
                    item.label === (locale === 'tr' ? 'HİZMETLERİMİZ' : 'SERVICES') ? 'hizmetlerimiz' :
                    'faydali-linkler'
                  }')`}
                >
                  <svg class="w-4 h-4 transition-transform" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
                  </svg>
                </button>
              )}
            </div>
            
            {/* Mobile Dropdown Menus */}
            {(item as any).hasDropdown && item.label === (locale === 'tr' ? 'KURUMSAL' : 'CORPORATE') && (
              <div id="mobile-dropdown-kurumsal" class="hidden pl-4 mt-2 space-y-2 border-l-2 border-gray-200">
                <a href={`/${locale}/${locale === 'tr' ? 'hakkimizda' : 'about-us'}/`} class="block text-sm text-gray-600 hover:text-primary transition-colors py-1">
                  {locale === 'tr' ? 'Hakkımızda' : 'About Us'}
                </a>
                <a href={`/${locale}/${locale === 'tr' ? 'kronoloji' : 'chronology'}/`} class="block text-sm text-gray-600 hover:text-primary transition-colors py-1">
                  {locale === 'tr' ? 'Kronoloji' : 'Chronology'}
                </a>
                <a href="/tr/ekibimiz/" class="block text-sm text-gray-600 hover:text-primary transition-colors py-1" data-cache-bust={Date.now()} onclick="console.log('Ekibimiz link clicked: /tr/ekibimiz/'); return true;">
                  {locale === 'tr' ? 'Ekibimiz' : 'Our Team'}
                </a>
              </div>
            )}
            
            {(item as any).hasDropdown && item.label === (locale === 'tr' ? 'FUARLARIMIZ' : 'OUR FAIRS') && (
              <div id="mobile-dropdown-fuarlarimiz" class="hidden pl-4 mt-2 space-y-2 border-l-2 border-gray-200">
                {activeFairYears.map(fairYear => {
                  const slug = locale === 'tr' 
                    ? (fairYear.data.page_slug || `${fairYear.data.year}-fuar-takvimi`)
                    : (fairYear.data.page_slug_en || `${fairYear.data.year}-fair-calendar`);
                  const menuTitle = locale === 'tr' 
                    ? (fairYear.data.page_content?.menu_title_tr || `${fairYear.data.year} Fuar Takvimi`)
                    : (fairYear.data.page_content?.menu_title_en || `${fairYear.data.year} Fair Calendar`);
                  
                  return (
                    <a href={`/${locale}/${slug}/`} class="block text-sm text-gray-600 hover:text-primary transition-colors py-1">
                      {menuTitle}
                    </a>
                  );
                })}
                <a href={`/${locale}/${locale === 'tr' ? 'galeri' : 'gallery'}/`} class="block text-sm text-gray-600 hover:text-primary transition-colors py-1">
                  {locale === 'tr' ? 'Galeri' : 'Gallery'}
                </a>
              </div>
            )}
            
            {(item as any).hasDropdown && item.label === (locale === 'tr' ? 'HİZMETLERİMİZ' : 'SERVICES') && (
              <div id="mobile-dropdown-hizmetlerimiz" class="hidden pl-4 mt-2 space-y-2 border-l-2 border-gray-200">
                <a href={`/${locale}/${locale === 'tr' ? 'nakliye' : 'shipping'}/`} class="block text-sm text-gray-600 hover:text-primary transition-colors py-1">
                  {locale === 'tr' ? 'Nakliye' : 'Shipping'}
                </a>
                <a href="https://turkelglobalstands.com" target="_blank" rel="noopener noreferrer" class="block text-sm text-gray-600 hover:text-primary transition-colors py-1">
                  {locale === 'tr' ? 'Stant Hizmeti' : 'Booth Service'}
                </a>
                <a href={`/${locale}/${locale === 'tr' ? 'turizm' : 'tourism'}/`} class="block text-sm text-gray-600 hover:text-primary transition-colors py-1">
                  {locale === 'tr' ? 'Turizm' : 'Tourism'}
                </a>
              </div>
            )}
            
            {(item as any).hasDropdown && item.label === (locale === 'tr' ? 'FAYDALI LİNKLER' : 'USEFUL LINKS') && (
              <div id="mobile-dropdown-faydali-linkler" class="hidden pl-4 mt-2 space-y-2 border-l-2 border-gray-200">
                <a href="https://www.ticaret.gov.tr/yurtdisi-teskilati" target="_blank" class="block text-sm text-gray-600 hover:text-primary transition-colors py-1">
                  {locale === 'tr' ? 'Müşavirlikler' : 'Consultancies'}
                </a>
                <a href="https://www.mfa.gov.tr/yurtdisi-teskilati.tr.mfa" target="_blank" class="block text-sm text-gray-600 hover:text-primary transition-colors py-1">
                  {locale === 'tr' ? 'Büyükelçilikler' : 'Embassies'}
                </a>
                <a href="https://www.ticaret.gov.tr/" target="_blank" class="block text-sm text-gray-600 hover:text-primary transition-colors py-1">
                  {locale === 'tr' ? 'T.C. Ticaret Bakanlığı' : 'Ministry of Trade'}
                </a>
              </div>
            )}
          </div>
        ))}
        
        <!-- Mobile CTA Buttons -->
        <div class="flex flex-row gap-3 pt-6 border-t border-gray-200 pb-8 px-4">
          <button 
            onclick="openVisitorModal()"
            class="btn btn-primary text-xs px-4 py-2 whitespace-nowrap flex-1 text-center"
          >
            {locale === 'tr' ? 'Ziyaretçi Formu' : 'Visitor Form'}
          </button>
          <button 
            onclick="openParticipantModal()"
            class="btn btn-outline text-xs px-4 py-2 whitespace-nowrap flex-1 text-center"
          >
            {locale === 'tr' ? 'Katılımcı Formu' : 'Participant Form'}
          </button>
        </div>
      </nav>
    </div>
  </div>
</header>

<!-- Form Modals -->
<VisitorFormModal locale={locale} />
<ParticipantFormModal locale={locale} />

<style>
  /* Force dropdown visibility on hover */
  .group:hover .group-hover\:opacity-100 {
    opacity: 1 !important;
  }
  
  .group:hover .group-hover\:visible {
    visibility: visible !important;
  }
  
  .group:hover .group-hover\:translate-y-0 {
    transform: translateY(0) !important;
  }
  
  .group:hover .group-hover\:rotate-180 {
    transform: rotate(180deg) !important;
  }
  
  .group:hover .group-hover\:w-full {
    width: 100% !important;
  }
  
  /* Mobile form button colors - force override */
  .btn.btn-primary {
    background-color: #CE1A28 !important;
    color: white !important;
    border: 1px solid #CE1A28 !important;
  }
  
  .btn.btn-primary:hover {
    background-color: #B01525 !important;
  }
  
  .btn.btn-outline {
    background-color: transparent !important;
    color: #CE1A28 !important;
    border: 1px solid #CE1A28 !important;
  }
  
  .btn.btn-outline:hover {
    background-color: #CE1A28 !important;
    color: white !important;
  }

  /* Small laptop optimization (1366x768 etc) */
  @media (min-width: 1024px) and (max-width: 1366px) {
    /* Compact navigation for small laptops */
    .lg\\:flex .text-sm {
      font-size: 0.75rem !important; /* 12px */
    }
    
    /* Tighter navigation spacing */
    .lg\\:space-x-8 > * + * {
      margin-left: 1rem !important; /* 16px */
    }
    
    /* Smaller CTA buttons */
    .lg\\:space-x-3 .btn {
      padding: 0.4rem 0.8rem !important;
      font-size: 0.7rem !important;
    }
    
    /* Compact logo */
    .lg\\:w-\\[200px\\] {
      width: 160px !important;
    }
    
    /* Language switcher compact */
    .lg\\:ml-4 {
      margin-left: 0.5rem !important;
    }
  }

  /* 1440x900 specific optimization - balanced sizing */
  @media (min-width: 1366px) and (max-width: 1440px) {
    /* Smaller logo with closer positioning */
    header img[alt="Türkel Fuarcılık"] {
      width: 140px !important;
      height: auto !important;
      margin-left: 8px !important; /* Closer to edge */
    }
    
    /* Compact but readable menu text */
    header nav a {
      font-size: 0.75rem !important; /* 12px - readable */
      padding: 0.375rem 0.625rem !important; /* 6px 10px */
    }
    
    /* Tighter navigation container spacing */
    header nav {
      gap: 0.25rem !important; /* Very tight */
      margin-left: -1rem !important; /* Pull closer to logo */
    }
    
    /* Compact form buttons */
    header button[onclick*="Modal"] {
      padding: 0.375rem 0.75rem !important; /* 6px 12px */
      font-size: 0.7rem !important; /* 11.2px */
      min-height: auto !important;
      white-space: nowrap !important;
    }
    
    /* Right side button container - closer spacing */
    header .hidden.lg\\:flex.items-center {
      gap: 0.5rem !important; /* 8px */
      margin-right: 8px !important;
    }
    
    /* Slightly tighter header padding */
    header .flex.items-center.py-2 {
      padding-top: 0.5rem !important;
      padding-bottom: 0.5rem !important;
    }
    
    /* Force navigation text to be readable */
    header nav a span,
    header nav a,
    header nav button span,
    header nav button {
      font-size: 0.75rem !important; /* 12px */
    }
    
    /* Dropdown menu items compact but readable */
    header .absolute.bg-white a {
      font-size: 0.7rem !important; /* 11.2px */
      padding: 0.375rem 0.625rem !important;
    }
    
    /* Button text readable */
    header button span {
      font-size: 0.7rem !important; /* 11.2px */
    }
    
    /* Navigation center adjustment */
    header nav.hidden.lg\\:flex {
      justify-content: flex-start !important;
      margin-left: 1rem !important; /* Start closer to logo */
    }
  }

  /* Modern fluid header optimization for larger screens */
  @media (min-width: 1440px) {
    /* Fluid font sizing for navigation */
    .lg\\:flex .text-sm {
      font-size: clamp(0.8rem, 0.7rem + 0.3vw, 1rem);
    }
    
    /* Fluid spacing for navigation */
    .lg\\:space-x-8 > * + * {
      margin-left: clamp(1.5rem, 2vw, 2rem);
    }
    
    /* Fluid CTA button sizing */
    .lg\\:space-x-3 .btn {
      padding: clamp(0.6rem, 0.5rem + 0.2vw, 0.75rem) clamp(1.2rem, 1rem + 0.3vw, 1.5rem);
      font-size: clamp(0.8rem, 0.7rem + 0.2vw, 0.875rem);
    }
    
    /* Fluid logo sizing */
    .lg\\:w-\\[200px\\] {
      width: clamp(180px, 16vw, 200px);
    }
  }
  
  /* Strong dropdown visibility rules */
  .group:hover > div[class*="absolute"] {
    opacity: 1 !important;
    visibility: visible !important;
    transform: translateY(0) !important;
    display: block !important;
  }
  
  /* Make dropdown stick to header bottom */
  .group > div[class*="absolute"] {
    border-top: none !important;
    margin-top: 0 !important;
  }
  
  /* Keep dropdown visible when hovering over dropdown itself */
  .group > div[class*="absolute"]:hover {
    opacity: 1 !important;
    visibility: visible !important;
    transform: translateY(0) !important;
    display: block !important;
  }
  
  /* Remove all button default styles */
  .group button,
  nav button {
    background: none !important;
    border: none !important;
    padding: 0 !important;
    margin: 0 !important;
    font: inherit !important;
    color: inherit !important;
    text-decoration: none !important;
    cursor: pointer !important;
    outline: none !important;
    box-shadow: none !important;
  }
  
  /* Button hover effects */
  .group:hover button {
    color: #CE1A28 !important;
  }
  
  /* Ensure hover works on button elements */
  .group:hover button span {
    width: 100% !important;
  }
  
  /* Debug - temporary red border to see dropdown areas */
  .group > div[class*="absolute"] {
    /* border: 2px solid red !important; */
    /* background: yellow !important; */
  }
  
  /* Mobile dropdown text hover effect */
  .mobile-nav-item span:hover {
    color: #CE1A28 !important;
  }
  
  /* Mobile dropdown active state */
  .mobile-nav-item span:active {
    background-color: rgba(206, 26, 40, 0.1) !important;
  }
</style>

<script>
  // Mobile menu toggle
  const mobileMenuButton = document.getElementById('mobile-menu-button');
  const mobileMenu = document.getElementById('mobile-menu');
  
  if (mobileMenuButton && mobileMenu) {
    mobileMenuButton.addEventListener('click', () => {
      mobileMenu.classList.toggle('hidden');
    });
  }
  
  // Mobile dropdown toggle function
  function toggleMobileDropdown(menuId: string) {
    const dropdown = document.getElementById(`mobile-dropdown-${menuId}`);
    
    // Find arrow in the same mobile-nav-item container
    const mobileNavItem = dropdown?.closest('.mobile-nav-item');
    const arrow = mobileNavItem?.querySelector('.mobile-dropdown-toggle svg');
    
    if (dropdown) {
      dropdown.classList.toggle('hidden');
      
      // Rotate arrow
      if (arrow) {
        const arrowElement = arrow as HTMLElement;
        if (dropdown.classList.contains('hidden')) {
          arrowElement.style.transform = 'rotate(0deg)';
        } else {
          arrowElement.style.transform = 'rotate(180deg)';
        }
      }
    }
  }
  
  // Make function globally available
  (window as any).toggleMobileDropdown = toggleMobileDropdown;
  
  // Close mobile menu when clicking outside
  document.addEventListener('click', (e) => {
    if (mobileMenu && !mobileMenu.contains(e.target as Node) && !mobileMenuButton?.contains(e.target as Node)) {
      mobileMenu.classList.add('hidden');
    }
  });
  
  // Close mobile menu on window resize
  window.addEventListener('resize', () => {
    if (window.innerWidth >= 1024 && mobileMenu) {
      mobileMenu.classList.add('hidden');
    }
  });
</script>
